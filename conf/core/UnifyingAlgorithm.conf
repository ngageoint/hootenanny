{
  "#": "Only add entries here that have different default values than the options in ConfigOptions.asciidoc. Conflation",
  "#": "with the Unifying Algorithm is the default configuration, so there shouldn't be too many.",
  "#": "end"
}
