With the auto merge many poi to one poly matches setting only turned off, we expect the closer POI to be matched against the poly.  The amenity=theatre tag is lost due to the default behavior of overwrite tag merging.

