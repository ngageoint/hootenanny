This tests to make sure that railway conflation doesn't leave any orphaned secondary features that belong to relations. See Linha do Oeste.
Since railway conflation uses the Unifying road merging algorithm, the fix employed by this test should work for road data as well.

Not sure yet if some of the bridges in the secondary data connected to the rails should be carried over to the output or not.
