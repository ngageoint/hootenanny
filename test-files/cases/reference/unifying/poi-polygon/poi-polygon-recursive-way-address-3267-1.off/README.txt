See note in PoiPolygonConflateAddressTest to why this has temporarily been disabled.

This is a POI/Poly address test checking to see that a POI correctly matches on address with a poly 
using addr:housename as the house number.
