With the auto merge many poi to one poly matches setting only turned off, we expect the POI to be reviewed against both polygons.

Note that this is the same behavior as when the setting is turned on.

