{
  "base.config": "UnifyingAlgorithm.conf,Testing.conf",
  "#": "This setting is here to suppress the 'non-contiguous relation when extracting a network' message for this test.",
  "#": "It doesn't seem to be causing any harm but probably should be looked into at some point.",
  "log.warn.message.limit": "-1",
  "rubber.sheet.log.missing.requirements.as.warning": "false",
  "uuid.helper.repeatable" : "true",
  "writer.include.conflate.review.detail.tags": "false",
  "writer.include.debug.tags": "true",
  "#": "end"
}
