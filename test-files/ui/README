This tests the web services at a higher system level via URL invocation (higher level than Java services tests).

These files use the older way of calling node.js commands directly to stimulate tests.  The functional tests here 
should probably be moved into the new cucumber tests (#ngageoint/hootenanny-ui#25)/
