##
# This file is the source control version of the liquibase.properties. The first time
# you build this should be copied to liquibase.properties. You can then modify that
# file to suite your local build environment. If you make changes to
# liquibase.properties.orig please send an email out to all developers that there has
# been a change.
##

# connect to the database
driver: org.postgresql.Driver
url: jdbc:postgresql:${DB_NAME}
username: ${DB_USER}
password: ${DB_PASSWORD}
#Reference to the master changelog file
changeLogFile: db/db.changelog-master.json
